每个消息都要设置fromModule和destModule。
转发过程中要修改fromModule。


Greet:

server<->transfer:
发去的msg的消息体是serverId。
返回的是msg的id，代表收到了。



ACK:
client<->server
发去的msg的消息体是msgId。
无回复;



启动顺序:transfer > server > client。

client向server发起连接时自动发送greet消息,格式如下:
消息体:userId,userName,ClientId.