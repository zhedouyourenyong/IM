路由->选择器->缓存->zkclient

路由通过选择器选出一个服务器，返回给客户端。
选择器在缓存的基础上通过某种算法获取一个可用服务器。



路由层相当于微服务网关，是客户端请求的门户，是调用具体服务端的桥梁。
屏蔽底层细节，对外提供简单统一的调用方式，比如HTTP。

客户端发起请求时首先将进入网关，随后网关将请求路由到具体的服务端，路由的过程牵涉到路由算法。



路由层：
账户：注册，登录，下线功能
缓存用户登录状态和路由信息并对外提供查询接口